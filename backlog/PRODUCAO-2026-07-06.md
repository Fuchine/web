# Prontidão de produção — análise de 2026-07-06

Segunda onda da auditoria do mesmo dia ([AUDITORIA-2026-07-06.md]): a primeira
cobriu o **código**; esta cobre **operação e produto** — o que falta para
levar o Fuchine a produção. Organizada por **portão**, porque "produção" são
três momentos com exigências diferentes. **13 itens novos** (10 na varredura
inicial + 3 no aprofundamento do dia-do-deploy).

O runbook executável do dia do deploy (config, auth, dimensionamento,
sequência, smoke test) vive em **`docs/DEPLOY_CHECKLIST.md`**.

## Portão 1 — lançamento OSS (self-host, 1 usuário) · o gate da F1

**✅ Concluído pelo PR #19 (2026-07-09).** Todos os 11 itens deste portão foram
resolvidos e removidos do backlog (histórico em `git log`): deploy story
(Dockerfiles + Caddy/TLS), backups/restore, origin configurável da extensão,
fail-fast de env (`instrumentation.ts`), security headers + CSP, higiene de repo
OSS (SECURITY/CONTRIBUTING/DCO), error/not-found boundaries, `/api/health`,
versionamento do ciphertext BYOK, decisão de timezone e knob do pool de DB.

Ressalvas operacionais herdadas (não são código):
- **Extensão** — resolvida em código; falta o teste em browser real contra uma
  instância https ao vivo.

## Portão 2 — instância compartilhada (amigos/comunidade)

| Item | Esforço | Prioridade | Impacto (1 linha) |
|---|---|---|---|
| [catalog-visibility-decision.md](catalog-visibility-decision.md) | decisão + S | Média | Todos veem os vídeos de todos — correto pelo modelo (D3), mas precisa virar decisão declarada + hide persistido. |

> `account-deletion-missing` resolvido (2026-07-10): `DELETE /api/account` +
> dialog de confirmação por e-mail; cascade do schema limpa os dados do usuário.

Já filados na primeira onda e que pertencem a este portão:
[ai-endpoints-no-rate-limit.md](ai-endpoints-no-rate-limit.md) (o portão em si —
**resolvido 2026-07-12**: as 5 superfícies autenticadas + o magic link A1). As
caps + dedup atômico de `/api/import` e o
`stats-writers-daily-caps` (clamp + throttle de beacon/click) já foram feitos e
removidos do backlog. O hide/save persistido, mitigador do
[catalog-visibility-decision.md], foi **resolvido pelo PR #16** (video flags) —
restou só a decisão de produto.

## Portão 3 — cloud paga (F3, só com sinais de demanda)

Deliberadamente **sem itens**: LGPD/ToS, quotas por plano (`user_plan` já
existe no schema e nada o lê — correto), billing e medição por usuário só
entram com os sinais do §2.3 da arquitetura. Pré-requisito técnico já filado:
[llm-usage-metering.md](llm-usage-metering.md).

## O que foi verificado e está OK

- `.env.example` completo e bem comentado (todas as envs, incluindo a de
  cifragem e o split MT).
- `LICENSE` (AGPL-3.0) e `NOTICE` no lugar; JMdict/EDRDG atribuído
  corretamente (CC BY-SA), listas de frequência marcadas como user-supplied.
  Ajuste menor: a menção a kuromoji/IPADIC é vaga — a licença do IPADIC pede
  reprodução do aviso (fazer junto do T1.10).
- Healthchecks de Postgres/Redis no compose, volumes nomeados, worker com
  shutdown gracioso (SIGTERM/SIGINT).
- Auth.js com sessões em banco (revogação funciona), CSRF do framework, magic
  link com fallback de console em dev.

## Sequência sugerida para o lançamento

1. [deploy-story-missing.md] (o T1.10 real) → 2. [backups-restore-missing.md]
   + [encryption-key-ops.md] (dados seguros) → 3.
   [extension-hardcoded-origin.md] (o loop funciona contra a instância) → 4.
   [error-pages-missing.md] + [health-endpoints-missing.md] +
   [timezone-streaks-decision.md] (polimento operacional, cabem juntos numa
   sessão).
